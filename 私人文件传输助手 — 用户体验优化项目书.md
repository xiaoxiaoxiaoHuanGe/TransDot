# 私人文件传输助手 — 用户体验优化项目书

**文档状态：持续维护；第 11 节为 V1.0.2 已确认实施记录，其余未确认条目仍为建议稿**  
**适用基线：TransDot V1.0.2**  
**目标：在不增加部署负担、不把项目做臃肿的前提下，继续提升日常使用效率、反馈清晰度和容错体验。**

> 本文不是新的冻结规范，也不代表其中所有建议都必须实现。只有经过确认的项目，才应写入《私人文件传输助手 V1 — 技术设计冻结规范.md》并进入开发。

---

# 1. 总体判断

当前版本已经完成了核心闭环：

```text
配对
↓
文字 / 图片 / 文件传输
↓
实时同步
↓
搜索 / 下载 / 删除
↓
文件过期与容量清理
```

下一阶段不应继续堆叠“大功能”，而应把重点放在三个问题上：

1. **减少操作步骤**：让用户从其他应用发送内容时更快。
2. **提高操作确定性**：每次复制、上传、下载、失败和重试都有清晰反馈。
3. **降低误操作成本**：删除、批量上传、网络失败等场景更容易理解和恢复。

最值得优先投入的方向不是多人、云盘、后台推送或桌面客户端，而是：

```text
Android 系统分享入口
+
消息操作菜单与一键复制
+
更清晰的上传队列和失败恢复
+
时间线可读性与无障碍优化
```

---

# 2. 必须继续遵守的产品边界

所有后续优化默认遵守以下约束：

- 保持单个 Android Master + 单个 Windows 浏览器设备。
- 保持 Go 单体服务、SQLite、本地文件存储和单 Docker 容器。
- 保持 `/app/data` 为唯一持久化数据目录。
- 不新增 Redis、消息队列、对象存储、独立搜索服务或后台任务容器。
- 不为了“可能的未来”提前拆微服务或多个代码仓库。
- 能在客户端完成的体验优化，优先不修改后端和数据库。
- 不把首页改成 Dashboard；时间线继续是唯一视觉中心。
- 不做强依赖后台常驻、系统推送或 PWA Service Worker 的体验。
- 新增功能必须至少满足一项：减少步骤、减少等待焦虑、降低误操作、提高可访问性。

建议再增加三条工程控制线：

- 单次版本最多引入一个新的核心交互模型。
- 非必要不增加第三方依赖；优先使用 Android、Compose、浏览器原生能力。
- 每项功能必须提供失败状态和退出路径，不能只设计成功流程。

---

# 3. 优化建议总览

| 方向 | 用户收益 | 实现成本 | 后端变化 | 推荐级别 |
| --- | --- | --- | --- | --- |
| Android 系统“分享到传输助手” | 极高 | 中 | 无 | S |
| 消息操作菜单与一键复制 | 高 | 低 | 无 | S |
| 成功/失败反馈统一 | 高 | 低 | 无 | S |
| 无障碍、触摸区域与大字体适配 | 高 | 低至中 | 无 | S |
| 上传队列、单项重试与取消 | 高 | 中 | 可选一个轻量接口 | A |
| 日期分隔、文件过期信息优化 | 中高 | 低 | 无 | A |
| Web 标签页未读数与快捷键 | 中 | 低 | 无 | A |
| 连接诊断与错误自助处理 | 中高 | 低至中 | 无 | A |
| 常用文本模板或收藏 | 中 | 中 | 可能需要数据库 | B |
| 浏览器系统通知 | 中低 | 中 | 无，但有权限干扰 | C |

其中：

- **S**：建议优先进入下一版本。
- **A**：值得做，但可以在 S 级完成后安排。
- **B**：有使用价值，需要确认真实使用频率。
- **C**：暂不推荐，容易增加干扰或维护成本。

---

# 4. S 级：下一步最值得做的优化

## 4.1 Android 系统分享入口

### 当前问题

用户在相册、文件管理器、浏览器或其他应用看到内容后，目前需要：

```text
退出当前应用
↓
打开传输助手
↓
点击附件
↓
重新找到刚才的文件
↓
发送
```

这是当前最明显的操作成本。

### 建议体验

让“传输助手”出现在 Android 系统分享面板中：

```text
相册 / 文件管理器 / 浏览器
↓
分享
↓
传输助手
↓
预览待发送内容
↓
确认发送
```

支持：

- 单个或多个图片。
- 单个或多个普通文件。
- 选中的文字或网页链接。

Android 官方提供 `ACTION_SEND`、`ACTION_SEND_MULTIPLE` 和 Content URI 接收机制，可复用当前上传服务，不需要新增服务器基础设施。参考：[Receive simple data from other apps](https://developer.android.com/training/sharing/receive)。

### 简化原则

- 不自动上传，必须先显示确认页，避免误分享。
- 不增加“联系人/会话选择”，因为项目只有一条时间线。
- 不做后台上传服务；进入应用后沿用现有前台上传流程。
- 分享文字时进入输入框，允许编辑后再发送。
- 分享文件时只保留一份 URI 列表，不复制到应用私有缓存。

### 验收标准

- 从系统相册分享 1–20 张图片可以进入预览并发送。
- 从文件管理器分享 PDF、ZIP、APK 等文件可以发送。
- 从浏览器分享链接时进入文字输入框，而不是直接发送。
- URI 无权读取、文件过大或批次超限时，给出明确原因并允许返回。

---

## 4.2 消息操作菜单与一键复制

### 当前问题

- 文字消息缺少明确的“复制”动作。
- 删除按钮长期显示，会增加视觉噪音，也容易让危险操作显得过于普通。
- Android 长按消息更接近删除入口，缺少符合直觉的操作菜单。

### 建议体验

点击或长按消息后打开轻量操作菜单：

**文字消息：**

```text
复制文字
删除消息
```

**文件消息：**

```text
下载
复制文件名
删除消息
```

**图片消息：**

```text
查看原图
下载
删除消息
```

复制成功后显示短暂提示：

```text
已复制
```

### 简化原则

- 不加入转发、回复、引用、表情反应或多选管理。
- 删除继续使用现有确认流程，不实现复杂撤销事务。
- Web 使用原生 Clipboard API；Android 使用系统 ClipboardManager。
- Android 文字也可以启用 Compose 原生选择能力，但“一键复制”作为主要路径更适合短消息。Compose 默认文字并不可选择，官方推荐通过文本选择容器或明确动作提供复制能力。参考：[Enable user interactions in text](https://developer.android.com/develop/ui/compose/text/user-interactions)。

### 验收标准

- 两端均可在两次操作以内复制文字。
- 删除仍需要明确确认。
- 操作菜单不会遮住当前消息，键盘弹出时也能正常显示。

---

## 4.3 成功、失败和恢复反馈统一

### 当前问题

应用已有错误信息和上传进度，但不同动作的完成反馈仍可进一步统一。用户可能不知道：

- 文字是否已复制。
- 文件是否已保存成功。
- 重新连接是否正在执行。
- 某次失败应该重试、重新选择文件还是检查服务器。

### 建议体验

建立一套非常小的反馈规则：

| 场景 | 反馈 |
| --- | --- |
| 复制成功 | 短时操作提示胶囊：“已复制” |
| Android 文件保存成功 | “已保存到所选位置” |
| 上传失败 | 显示原因 + “重试” |
| 网络离线 | 顶部状态显示“等待重连” + 手动重试入口 |
| 认证失效 | 明确说明“设备已被替换”，不显示模糊网络错误 |
| 文件过期 | 卡片说明“原文件已清理，消息记录仍保留” |

### 简化原则

- 不建立全局通知中心。
- 同一时间最多显示一条轻量操作提示浮层。
- 成功提示自动消失；需要操作的错误保留到用户处理或关闭。
- 继续依赖现有错误码，不解析英文错误文本。

---

## 4.4 无障碍与易点击性优化

这不是单独面向少数人的“附加功能”，而是会直接改善所有用户的点击、识别和操作稳定性。

建议检查：

- 所有图标按钮都有可读名称，TalkBack 不朗读成“未命名按钮”。
- 自定义文件卡片、图片宫格和状态指示具备正确语义。
- 可点击区域至少保持 48dp，避免小图标难点中。
- 系统字体放大后，输入框、文件名和按钮不重叠。
- 浅色和深色主题中的状态色具备足够对比度。
- Web 支持键盘焦点、可见焦点框和合理的 Tab 顺序。

Compose Material 组件已经提供部分默认语义，但自定义组件仍需补充描述和状态；Android 官方也建议交互目标至少达到 48dp。参考：[Accessibility in Jetpack Compose](https://developer.android.com/develop/ui/compose/accessibility)、[API defaults and minimum touch targets](https://developer.android.com/develop/ui/compose/accessibility/api-defaults)。

### 验收标准

- Android 使用 TalkBack 可以完成发送、下载、复制和删除。
- 字体放大到 1.3–1.5 倍后主要页面不截断关键操作。
- Web 只用键盘可以完成搜索、输入、发送和关闭弹层。

---

# 5. A 级：第二批体验增强

## 5.1 上传队列、单项重试与取消

### 建议体验

批量选择后显示一个轻量队列：

```text
等待上传
上传中 42%
已完成
失败 · 重试
```

建议支持：

- Android 按单个失败项重试，不必重新发送整个批次。
- 上传过程中可以取消当前批次。
- 失败项保留在界面中，直到用户重试或关闭。
- 继续保持顺序上传，不引入复杂并发调度。

### 后端取舍

如果只做“失败单项重试”，可以完全复用现有接口。

如果要做真正的“取消批次并立即释放预留空间”，建议只增加一个轻量接口：

```text
DELETE /api/v1/upload-batches/{id}
```

作用仅限：

```text
停止未完成Ticket
+
删除.part
+
释放reserved_bytes
```

不引入新的状态服务或后台系统。

---

## 5.2 时间线日期分隔与批次可读性

建议增加：

- “今天 / 昨天 / 2026年8月13日”日期分隔。
- 日期分隔随滚动自然出现，不做复杂吸顶动画也可以。
- 文件剩余时间从固定“24小时后过期”改为相对时间，如“剩余 3 小时”。
- 剩余不足 1 小时时使用警示色，但不弹窗打扰。
- 同一上传批次可以显示一个小型批次标题，如“3 个文件 · 共 28 MB”。

不建议加入：

- 聊天气泡已读状态。
- “对方正在输入”。
- 回复、引用和消息线程。
- 按月份折叠的复杂历史导航。

---

## 5.3 Web 标签页未读数与快捷键

这是低成本、低风险的 Web 增强。

建议：

- 页面在后台且收到新消息时，将标题变为：`(3) 传输助手`。
- 用户重新查看页面或回到底部后清零。
- `Ctrl/Command + K` 打开搜索。
- `Escape` 关闭搜索、附件面板和图片查看器。
- 保持现有 Enter 发送、Shift+Enter 换行逻辑。

不建议本阶段申请浏览器系统通知权限：

- 会增加权限提示和用户理解成本。
- 页面关闭后的可靠通知需要 Service Worker/PWA 或额外推送体系。
- 当前项目定位并不要求后台即时通知。

---

## 5.4 简单连接诊断

只在设置或错误状态中提供，不把首页改成 Dashboard。

建议显示：

```text
应用版本
服务器地址
服务是否可达
当前实时连接状态
最近一次同步时间
```

提供两个动作：

```text
重新连接
复制诊断信息
```

复制出的诊断信息必须自动排除：

- Master Token。
- Browser Token。
- Cookie。
- 配对 Secret。
- 文件名和消息内容。

这样用户遇到问题时可以更容易描述现象，又不需要加入日志上传、遥测平台或远程运维服务。

---

# 6. B 级：有真实需求后再做

## 6.1 常用文本模板

例如常用地址、命令、设备 IP 或固定说明。

建议只有在确认用户经常重复发送相同文本时再做，并优先使用 Android 本地存储，不急于同步到服务器。

控制范围：

- 最多 10–20 条。
- 只支持标题和文本。
- 不做分类、标签、云同步和模板市场。

## 6.2 文件卡片快速预览

可以考虑仅预览纯文本和 PDF，但该方向会引入 MIME、安全、编码和大文件读取问题。

建议优先观察“下载后打开”是否真的造成明显痛点。若没有高频需求，继续保留当前文件卡片 + 下载方式更简单、安全。

## 6.3 简单收藏

收藏文字消息可能有价值，但会引入新的持久化状态、搜索条件和 UI 入口。

只有当“长期保存常用文字”成为高频需求时再考虑；不要把它扩展成笔记系统。

---

# 7. 明确不建议进入近期路线图

以下功能会明显偏离当前产品定位：

- 多用户、账号系统、登录注册、权限角色。
- 同时配对多台 Android 或多台 Windows。
- Electron/Windows 桌面客户端。
- PWA、Service Worker、后台推送平台。
- Redis、消息队列、对象存储和独立搜索服务。
- 文件夹、网盘目录、永久文件管理和双向目录同步。
- 离线消息发送队列和后台自动重传服务。
- 消息回复、引用、表情、已读回执、正在输入等聊天社交能力。
- 在线 Office/PDF/视频全格式预览。
- 服务端压缩大批文件为 ZIP。
- 自动更新 APK、应用商店分发体系。
- 用户行为埋点、云端统计和第三方遥测 SDK。

这些功能并非永远不能做，但必须有明确、持续、真实的需求证明，且应作为独立项目重新评估，而不是顺手加入体验版本。

---

# 8. 推荐实施路线

## 第一阶段：V1.0.2 轻量操作优化

建议内容：

- 消息操作菜单。
- 一键复制文字/文件名。
- 统一轻量操作提示与错误动作。
- Web 标签页未读数和快捷键。
- Android/Web 无障碍与点击区域检查。

特点：

```text
不改后端
不改数据库
不改部署
风险最低
```

## 第二阶段：V1.1.0 快速分享

建议内容：

- Android 接收系统分享的文字、图片和文件。
- 分享内容确认页。
- Android 上传失败单项重试。
- 必要时增加批次取消接口。

这是对日常使用效率提升最大的一版，建议作为下一次主要功能版本。

## 第三阶段：V1.1.1 时间线与诊断完善

建议内容：

- 日期分隔。
- 文件剩余时间提示。
- 简单连接诊断和复制诊断信息。
- 根据实机使用反馈修正字体、触摸、深色模式和横竖屏细节。

---

# 9. 每个优化版本的统一验收门槛

任何后续体验版本都应满足：

1. `docker compose up --build -d` 仍是唯一服务部署方式。
2. Docker 内仍只有一个业务服务进程。
3. 原 `/app/data` 数据可直接升级使用。
4. Web、Go、Android 全量构建和测试通过。
5. Android 主要流程在实机完成验收。
6. Web 完成桌面、窄屏、键盘和深色模式检查。
7. 不记录 Token、Cookie、Secret、文件内容或消息内容到日志。
8. 新功能失败时不会阻断已有文字、图片和文件传输。
9. 新增依赖、接口或数据表必须在交付说明中逐项解释其必要性。

---

# 10. 最终建议

如果只选择一个下一步方向，推荐：

> **优先完成 Android 系统“分享到传输助手”，同时补齐消息复制与操作菜单。**

原因：

- 它直接减少每天最常见的发送步骤。
- 不需要新增服务、数据库或部署组件。
- 可以最大程度复用现有上传、消息和认证逻辑。
- 完成后用户会明显感觉应用“更像系统的一部分”，而不是必须先打开再操作的独立工具。

第二优先级是：

> **上传失败恢复、日期分隔、统一反馈和无障碍。**

这些优化不会改变产品结构，却能让长期使用更稳定、更顺手。

---

# 11. V1.0.2 已确认与实施记录

本节记录用户在实机与网页体验后明确确认的优化决策。若本节与前文建议方案存在差异，以本节为准。

## 11.1 网页图片查看器

- 图片放大后必须在当前可用视口内完整显示，使用等比 `contain`，不得裁切画面。
- 原图过期时仍可按相同规则显示缩略图预览。
- 保留返回、下载、键盘 `Escape` 和左右切换。
- 点击图片周围的黑色空白区域可以直接退出；点击图片本身不得误退出。

## 11.2 Web 消息复制与删除

- 每个可视对话单元显示两个小型图标按钮：复制、删除。
- 操作按钮始终放在靠时间线中央的一侧：Windows 自己的右侧消息放在气泡左边；Android 对端的左侧消息放在气泡右边。
- 按钮采用低对比边框与紧凑尺寸；删除仅在悬停时强调危险色，保持页面简洁。
- 文字复制原文；文件复制原始文件名；多图批次复制该批次所有文件名。
- 删除继续显示明确确认；多图批次作为一个对话单元整组确认和删除。
- 不增加转发、回复、收藏、表情、多选等其他消息操作。

## 11.3 Android 消息复制与删除

- 文字、文件和图片组在消息内容底部右侧显示复制与垃圾桶图标。
- 文字复制原文；文件复制原始文件名；图片组复制所有原始文件名。
- 删除继续复用现有确认流程；图片批次可整组删除。
- 操作成功通过短时操作提示胶囊反馈，不长期占用时间线空间。

## 11.4 Android 微信式输入区

- 底部输入区改为单行横向主布局：圆角输入框 + 右侧动作位。
- 输入框为空时右侧显示“＋”，点击后继续打开现有照片/文件选择器。
- 输入文字后，“＋”原位切换为“发送”按钮；键盘发送键继续有效。
- 不增加语音、表情、贴纸等聊天功能，只借鉴紧凑布局和动作切换逻辑。

## 11.5 Android 系统分享入口

- Android 声明接收 `ACTION_SEND` 与 `ACTION_SEND_MULTIPLE`，支持文字、链接、图片和普通文件。
- 分享文字只放入输入框，由用户编辑后发送，不自动发送。
- 分享文件先显示文件数量与名称确认框，确认后复用现有前台上传流程。
- 混合分享时文件进入确认流程，附带文字放入输入框。
- 不新增后台上传服务、通知常驻、联系人/会话选择、缓存复制或服务器接口。

## 11.6 统一反馈与边界

- Android 的复制、上传、删除和系统分享进入上传队列使用统一的应用内轻量操作提示浮层，不使用 Material 系统 Snackbar。
- 文字消息发送成功不显示操作提示，避免遮挡时间线；消息出现在时间线中即为成功反馈。
- Android 成功提示采用输入栏上方居中的紧凑胶囊样式：外层透明，深色半透明圆角胶囊仅按文字内容占用必要空间，最大宽度约 `280dp`，文字单行居中。
- 成功提示固定显示 `1.5` 秒；新提示立即替换旧提示并重新计时，不建立等待队列。提示浮层不得参与时间线布局，并须避让一键下拉按钮和下载进度提示。
- 错误仍保留明确原因和可恢复入口，成功提示自动消失。
- 本次不修改 Go 后端 API、SQLite、文件生命周期、`/app/data`、Docker 或 1Panel 部署架构。
- Android 版本升级为 `versionCode 6`、`versionName 1.0.2`。
