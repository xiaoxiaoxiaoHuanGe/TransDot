# 私人文件传输助手 V1 — 技术设计冻结规范

**状态：Frozen / V1 开发基线**

本规范覆盖此前讨论中已经修改的旧方案。后续开发如果没有新的明确决策，以本文件为准。

原始需求中的核心技术方向继续保留：Go + SQLite + WebSocket + React/TypeScript + Kotlin/Jetpack Compose，以及“文件临时、文字长期”的双生命周期模型。

---

# 1. 最终系统架构

采用尽可能简单的单体架构：

```text
realme GT5 Pro
Android 16 App
        │
        │ HTTPS / WSS
        ▼
     Internet
        │
        ▼
域名 :443
        │
1Panel
SSL终止
反向代理
        │
        ▼
Host :5757
        │
        ▼
┌───────────────────────────┐
│ Docker                    │
│                           │
│ Transfer Assistant        │
│                           │
│ Go Backend                │
│ + React静态文件           │
│ + WebSocket               │
│ + SQLite                  │
│ + 本地文件存储            │
│                           │
│ Listen :5757              │
└───────────────────────────┘
```

## 冻结决策

Docker 内：

```text
HTTP
WS
Port 5757
```

Docker **不处理**：

```text
HTTPS
SSL证书
443
域名证书续期
```

这些全部交给：

```text
1Panel
```

完成。

---

# 2. 推荐采用单容器，而不是前后端多个容器

V1 不建议：

```text
frontend container
backend container
nginx container
database container
```

建议最终只有：

```text
transfer-assistant
```

一个应用容器。

构建过程：

```text
React + TypeScript
        ↓
npm build
        ↓
静态文件
        ↓
Go embed
        ↓
单个Go程序
        ↓
Docker Image
```

Go 同时负责：

```text
/api/*
/ws
React静态资源
SPA fallback
```

优势：

```text
一个端口
一个容器
一个数据卷
没有CORS复杂度
没有前后端地址配置
没有额外Web服务器
```

非常适合本项目。

---

# 3. Docker运行规格

容器：

```text
0.0.0.0:5757
```

映射：

```text
5757:5757
```

数据目录：

```text
/app/data
```

建议结构：

```text
/app/data/
├── database/
│   └── transfer.db
│
├── files/
│   ├── ...
│
├── thumbs/
│   ├── ...
│
└── tmp/
    ├── ...
```

所有这些目录必须挂载持久化 Volume。

---

# 4. 1Panel最终访问模型

公网：

```text
https://你的域名
```

1Panel：

```text
https://你的域名:443
        ↓
http://127.0.0.1:5757
```

WebSocket：

```text
wss://你的域名/ws
        ↓
ws://127.0.0.1:5757/ws
```

前端代码禁止写死：

```text
服务器IP
5757
http://
ws://
```

Web统一使用：

```text
/api/v1/...
/ws
```

浏览器根据当前页面自动使用：

```text
HTTPS → WSS
HTTP  → WS
```

因此以后：

- 换服务器
- 换IP
- 换域名
- 换证书

都不需要重新编译Web。

---

# 5. 健康检查

Server提供：

```text
GET /healthz
```

正常：

```json
{
  "status": "ok"
}
```

不需要认证。

但不能返回：

- Token
- 数据库路径
- 文件列表
- 配置信息
- 系统敏感信息

---

# 6. Android技术基线

目标实机：

```text
realme GT5 Pro
Android 16
```

Android 16 对应 API Level 36，因此项目基线建议：

```text
compileSdk = 36
targetSdk = 36
```

Android 官方当前的 Android 16 SDK 文档也是按 API 36 配置。

UI：

```text
Kotlin
Jetpack Compose
Material 3
Single Activity
```

架构采用：

```text
UI
↓
ViewModel
↓
Repository
↓
API / WebSocket / Local State
```

Compose采用单向数据流：

```text
State ↓
UI

Event ↑
ViewModel
```

这与 Android 官方当前推荐的 Compose UDF 和分层应用架构一致。

---

# 7. Android Edge-to-Edge

V1必须正式支持 Edge-to-Edge。

不是后期美化项。

Android官方目前对面向较新Android版本的应用要求正确处理Edge-to-Edge，并特别强调System Bar、Gesture Insets、Display Cutout以及IME区域。

必须正确处理：

```text
Status Bar
Navigation Bar
Gesture Insets
Camera Cutout
IME / Keyboard
```

禁止：

```text
顶部内容顶进摄像头
底部输入框被键盘遮挡
按钮进入系统手势区域
```

---

# 8. Android页面结构

V1控制在少量核心页面。

```text
App
│
├── 首次初始化
│
├── 主时间线
│
├── 搜索
│
├── Windows配对
│
├── 图片Viewer
│
└── 简单设置
```

不要建立复杂导航树。

---

# 9. Android首次初始化

这里增加一个此前需求中缺失但安全上必须存在的机制。

问题：

如果一台全新Server：

```text
没有Android Master
```

而任何Android都能：

```text
POST /setup
```

成为主人，那么谁先找到服务器谁就可能抢占设备。

因此增加：

# OWNER_SETUP_TOKEN

部署容器时用户自行设置一次初始化密钥。

例如环境变量：

```text
OWNER_SETUP_TOKEN=<强随机字符串>
```

Android第一次启动：

```text
输入服务器地址
↓
输入初始化密钥
↓
Claim Server
↓
获得 Master Device Token
```

初始化完成以后：

```text
Server已有Master
```

此时无论别人是否知道：

```text
OWNER_SETUP_TOKEN
```

都不能再次Claim。

---

# 10. Android重新安装

已经确定：

```text
不提供账号恢复
```

如果Android卸载导致Master Token丢失：

V1不实现：

```text
Recovery Key
邮箱恢复
账号恢复
云恢复
```

如果真的需要从头开始：

用户通过1Panel自行清空应用数据目录并重新初始化。

这符合“私人自用工具、数据丢失可以接受”的前提。

---

# 11. Token设计

## Android Master Token

Server生成：

```text
32 bytes cryptographically random
```

客户端保存：

```text
Android安全存储
```

Server：

**绝不保存Token明文。**

只保存：

```text
SHA-256(token)
```

随机Token本身拥有足够高熵，因此这里不是用户密码Hash场景。

---

# 12. Windows认证方式

Windows Browser Token：

**不放 localStorage。**

使用：

```text
HttpOnly Cookie
Secure
SameSite=Strict
```

这样前端JavaScript无法直接读取长期Browser Token。

Cookie只用于：

```text
同源Web
```

Android则使用：

```text
Authorization: Bearer <master_token>
```

两种客户端的认证方式明确分离。

---

# 13. 配对协议

## Step 1

未配对Windows打开网页：

```text
POST /api/v1/pairing/sessions
```

Server生成：

```text
session_id
6位数字码
qr_secret
expires_at
```

---

# 14. 配对页面

Windows显示：

```text
┌──────────────────────┐
│                      │
│       QR CODE        │
│                      │
│       538 219        │
│                      │
│  请使用手机App配对    │
└──────────────────────┘
```

二维码内容：

```text
session_id
+
高熵 qr_secret
```

6位码只是手动输入备用方案。

---

# 15. Android扫码

Android：

```text
配对Windows
↓
扫码
↓
获得session
↓
向Server验证
```

如果当前没有Windows：

```text
直接确认配对
```

如果已经存在Windows：

```text
当前已有一台Windows设备
继续配对将使原设备失效

[取消] [替换]
```

只有用户点击：

```text
替换
```

才能继续。

---

# 16. Windows替换

Server必须保证替换是原子的：

```text
BEGIN TRANSACTION

Revoke Windows A
Activate Windows B

COMMIT
```

不能出现：

```text
Windows A有效
+
Windows B有效
```

的中间状态。

完成之后：

```text
Windows A
```

所有REST请求立即返回：

```text
401 Unauthorized
```

其WebSocket立即关闭。

---

# 17. Pairing Session安全

建议冻结：

```text
有效期：2分钟
```

6位码：

```text
每Session最多失败5次
```

超过：

```text
立即失效
```

同时进行IP级速率限制。

二维码里的高熵secret不是6位数字，因此扫码路径安全强度高于手输配对码。

---

# 18. 配对完成通知

未认证Windows配对页面**不建立主WebSocket**。

配对状态使用简单Polling：

```text
GET /api/v1/pairing/sessions/{id}/status
```

例如：

```text
每2秒
```

一次。

一旦：

```text
approved
```

Server通过该浏览器自己的Pairing Session验证后：

```text
Set-Cookie
```

写入Browser Token。

随后进入主页面并建立认证WebSocket。

这样比给未认证客户端设计一套特殊WebSocket协议简单。

---

# 19. 配对状态

```text
pending
approved
rejected
expired
consumed
```

成功以后：

```text
consumed
```

不能重复使用。

---

# 20. 消息数据模型

时间线以：

```text
message
```

作为统一入口。

建议：

```sql
messages
--------
id
type
batch_id
source_device_id
text_content
created_at
deleted_at
metadata_expires_at
```

type：

```text
text
image
file
```

---

# 21. 多图批次

用户一次选择：

```text
20张图片
```

不能在UI里表现为20条完全无关的聊天消息。

增加：

```text
batch_id
```

例如：

```text
Batch ABC

IMG001
IMG002
IMG003
...
```

UI发现：

```text
相邻消息
+
相同batch_id
+
type=image
```

即可绘制为图片宫格。

数据库仍然：

```text
一张图片 = 一个message
```

这样删除、过期和下载逻辑比较简单。

---

# 22. Files数据模型

建议：

```sql
files
-----
id
message_id
batch_id
original_filename
mime_type
size_bytes
storage_key
thumbnail_key
status
created_at
upload_completed_at
expires_at
expired_at
expired_reason
deleted_at
```

status：

```text
uploading
available
expired
failed
deleted
```

注意：

**不把 downloading 保存成持久状态。**

下载可能同时发生多次。

下载活动使用Server内存中的：

```text
active_download_count
```

管理。

---

# 23. persistent file state

状态机：

```text
          ┌──────── failed
          │
uploading
    │
    ▼
available
    │
    ├── TTL─────────┐
    │               │
    ├── Capacity────┤
    │               ▼
    │            expired
    │
    └── User delete
            ↓
         deleted
```

---

# 24. expired_reason

建议保存：

```text
ttl
capacity
```

用户界面都可以只显示：

```text
已过期
```

但是Server需要知道为什么过期，方便以后排查。

---

# 25. 文件限制精确定义

为了避免 `300M` 在代码中产生不同解释，开发内部固定为字节。

## 单文件

```text
300 × 1024 × 1024
= 314,572,800 bytes
```

UI显示：

```text
300 MB
```

## 单批

```text
500 × 1024 × 1024
= 524,288,000 bytes
```

UI：

```text
500 MB
```

## 临时文件池

```text
1024 × 1024 × 1024
= 1,073,741,824 bytes
```

UI：

```text
1 GB
```

---

# 26. Upload Batch

不要让20个上传请求各自独立地声称：

```text
我是合法批次
```

先创建Batch。

```text
POST /api/v1/upload-batches
```

Request：

```json
{
  "items": [
    {
      "filename": "IMG_001.jpg",
      "mime_type": "image/jpeg",
      "size_bytes": 5242880,
      "kind": "image"
    }
  ]
}
```

Server一次性检查：

```text
items <= 20
每个 <= 300MB
total <= 500MB
```

通过后创建上传Ticket。

---

# 27. 空间预留

这里不能只在文件上传结束后检查1GB。

否则：

```text
已有900MB
↓
同时接受两个500MB批次
```

最终一定超容量。

所以Server在创建Batch时计算：

```text
available_storage
+
active_reserved_uploads
+
new_batch
```

如果不足：

先执行容量回收。

仍不足：

```text
507 Insufficient Storage
```

拒绝创建批次。

---

# 28. Upload Reservation

每个Batch拥有：

```text
reserved_bytes
```

上传成功：

```text
reservation转为实际占用
```

取消/失败/长时间无活动：

```text
释放reservation
```

建议未完成Upload Session：

```text
30分钟
```

后视为过期并清理 `.part` 文件。

---

# 29. 文件上传

每一个Upload Ticket：

```text
PUT /api/v1/uploads/{upload_id}
```

Body：

```text
raw binary
```

客户端必须发送：

```text
Content-Length
```

Server：

```text
HTTP Body
↓
Limited Reader
↓
/data/tmp/<upload-id>.part
↓
实际大小校验
↓
fsync/close
↓
atomic rename
↓
/data/files/<random-storage-key>
```

严禁：

```text
io.ReadAll(300MB)
```

整个文件必须流式写入磁盘。

---

# 30. 上传成功

只有：

```text
完整接收
+
大小正确
+
文件成功落盘
```

以后：

```text
status = available
upload_completed_at = now
expires_at = now + 24h
```

然后创建时间线消息。

因此发送端看到：

```text
上传成功
```

的含义非常明确：

> Server已经完整保存文件。

---

# 31. 上传进度

上传进度主要由：

```text
发送端HTTP client
```

计算。

不需要Server通过WebSocket每100KB广播进度。

另一端只需要：

```text
上传完成以后
```

收到：

```text
message.created
```

即可。

这样大幅减少WebSocket噪音。

---

# 32. 图片缩略图

因为时间线可能一次展示20张照片，不能让接收端为了显示缩略图就下载20张原图。

因此V1增加：

```text
thumbnail
```

客户端上传图片时同时产生轻量预览。

建议：

```text
最长边 720px
JPEG
```

缩略图仅用于：

```text
时间线
图片Viewer初始预览
```

点击查看原图时再请求原文件。

原图：

```text
不二次压缩
```

仍完全保持用户选择的文件。

---

# 33. Thumbnail生命周期

缩略图非常小，可以随：

```text
文件消息
```

保存30天。

因此：

```text
原图
最长24h
```

但：

```text
thumbnail
最长30天
```

这样过期后的时间线仍然能辨认：

> 我之前传过什么图片。

30天消息清理时：

```text
message
+
thumbnail
```

一起删除。

Thumbnail也必须计入磁盘管理，但可以与1GB原文件池分开统计。

---

# 34. 不支持预览的文件

普通：

```text
PDF
DOCX
XLSX
ZIP
APK
EXE
PSD
...
```

全部：

```text
文件卡片
+
下载按钮
```

Server不解析内容。

---

# 35. REST API命名空间

统一：

```text
/api/v1
```

不要把V1接口直接放：

```text
/messages
/files
```

以免以后升级困难。

---

# 36. Setup API

```text
GET  /api/v1/setup/status
POST /api/v1/setup/claim
```

`status`：

```json
{
  "initialized": true
}
```

绝不能返回Master相关信息。

---

# 37. Pairing API

建议：

```text
POST /api/v1/pairing/sessions

GET  /api/v1/pairing/sessions/{id}/status

POST /api/v1/pairing/approve

POST /api/v1/pairing/reject
```

---

# 38. Timeline API

```text
GET /api/v1/messages
```

参数：

```text
before
limit
```

默认：

```text
50
```

最大也限制：

```text
50
```

避免客户端请求：

```text
limit=100000
```

---

# 39. 文本消息

```text
POST /api/v1/messages/text
```

限制：

```text
UTF-8
≤ 100KB
```

空字符串拒绝。

---

# 40. 删除

统一：

```text
DELETE /api/v1/messages/{message_id}
```

Text：

```text
删除消息
```

File/Image：

```text
删除message
+
原文件
+
thumbnail
```

---

# 41. Download

```text
GET /api/v1/files/{file_id}/download
```

必须：

```text
authentication
```

禁止静态暴露：

```text
/data/files
```

Response：

```text
Content-Disposition: attachment
```

原文件名仅用于Header展示。

绝不能作为磁盘路径。

---

# 42. Thumbnail API

```text
GET /api/v1/files/{file_id}/thumbnail
```

同样必须认证。

不能：

```text
/files/thumb123.jpg
```

公开访问。

---

# 43. Search API

```text
GET /api/v1/search?q=...
```

搜索：

```text
text_content
original_filename
```

使用：

```text
SQLite FTS5
```

原始需求已经指定第一版可以使用 SQLite + FTS5。

---

# 44. 搜索定位

点击搜索结果：

```text
GET /api/v1/messages/{id}/context
```

Server返回：

```text
目标消息
+
前20
+
后20
```

客户端滚动到：

```text
target_message_id
```

---

# 45. SQLite核心表

冻结建议：

```text
app_state
devices
pairing_sessions
upload_batches
files
messages
message_search_fts
```

不引入：

```text
users
accounts
conversations
contacts
rooms
```

因为产品根本不存在这些概念。

---

# 46. devices

```text
id
device_type
token_hash
created_at
last_seen_at
revoked_at
```

device_type：

```text
android_master
windows_browser
```

Server业务约束：

```text
最多一个有效android_master
最多一个有效windows_browser
```

---

# 47. FTS

FTS内容：

```text
message_id
text_content
original_filename
```

Message删除必须在同一个数据库事务中：

```text
删除普通记录
+
删除FTS记录
```

防止搜索出现幽灵结果。

---

# 48. 时间线游标

不使用：

```sql
OFFSET 50000
```

使用稳定游标：

```text
created_at
+
id
```

例如：

```text
before_created_at
before_id
```

避免以后数据多了以后OFFSET越来越慢。

---

# 49. WebSocket

入口：

```text
GET /ws
```

Web：

```text
Cookie Authentication
```

Android：

```text
Bearer Token
```

---

# 50. WebSocket事件Envelope

统一：

```json
{
  "event_id": "uuid",
  "type": "message.created",
  "timestamp": "...",
  "data": {}
}
```

第一版事件：

```text
message.created
message.deleted

file.expired

device.replaced
```

不要一开始设计几十种事件。

---

# 51. WebSocket不是数据库

这一点必须写进Codex规则：

> WebSocket只是实时通知渠道，不是真相来源。

例如Windows断网10分钟：

```text
期间漏掉5条WebSocket消息
```

重新连接后不能假设：

```text
什么都没发生
```

必须：

```text
WebSocket reconnect
↓
REST重新同步最近时间线
↓
reconcile
```

SQLite / REST才是：

```text
Source of Truth
```

---

# 52. Android后台行为

V1不承诺：

```text
App被系统杀死后
仍然实时收到全部WebSocket事件
```

Android官方目前也区分前台WebSocket实时通信与后台可靠消息机制；持久后台实时连接并不是应该随意依赖的模式。

因此V1：

```text
App前台
→ WebSocket保持连接
```

App重新进入前台：

```text
REST同步
+
重新连接WebSocket
```

即可。

这与你“不需要PWA、不要求后台即时通知”的产品取舍一致。

---

# 53. Android Photo Picker

多图选择使用Android系统Photo Picker。

V1：

```text
最大20张
```

Android官方Photo Picker接口原生支持设置多选上限，因此不需要自行实现媒体库选择器。

不申请整库照片读取权限作为主要方案。

---

# 54. Android Design System

UI质量属于：

**V1验收标准**

不是：

```text
有空再美化
```

设计方向：

```text
Modern Minimal
Material 3基础
克制
干净
高质量动效
```

避免：

```text
廉价渐变
满屏玻璃拟态
过度发光
复杂3D
过度动画
```

这个App本质还是效率工具。

---

# 55. Spacing

采用：

```text
4dp基础单位
8dp主节奏
```

常用：

```text
4
8
12
16
24
32
```

禁止随意出现：

```text
13dp
19dp
27dp
```

等无体系数字。

---

# 56. 圆角体系

建议：

```text
Small   10–12dp
Medium  16dp
Large   20–24dp
```

例如：

```text
文件卡片 16dp
输入区域 20–24dp
Bottom Sheet 顶部24dp
图片缩略图 12–16dp
```

不要所有组件统一一个圆角。

---

# 57. Motion规范

动画目的：

```text
反馈
层级
连续性
```

不是炫技。

建议三个主要时长：

```text
Fast     160–180ms
Normal   220–260ms
Emphasis 300–340ms
```

上传进度：

```text
连续平滑变化
```

成功：

```text
Progress
↓
Available icon
```

进行轻微Morph/Fade。

---

# 58. 动画必须覆盖的交互

V1至少认真设计：

```text
页面进入
Bottom Sheet
键盘弹出
发送消息
消息插入
上传进度
上传成功
删除消息
搜索进入
图片Viewer
左右滑图
配对成功
Windows被替换
```

---

# 59. 禁止的动画

禁止：

```text
每条消息不停浮动
持续呼吸光
背景粒子
大面积循环渐变
按钮永久闪烁
```

静止状态应该保持安静。

---

# 60. 首页布局

核心结构：

```text
┌──────────────────────────────┐
│  传输助手                 搜索 │
│  Windows · 已连接             │
├──────────────────────────────┤
│                              │
│         Timeline             │
│                              │
│   图片组                     │
│                              │
│               文本消息        │
│                              │
│   文件卡片                    │
│                              │
├──────────────────────────────┤
│   +   输入内容……        发送   │
└──────────────────────────────┘
```

重点：

**时间线永远是视觉中心。**

不要把首页做成Dashboard。

---

# 61. 图片组

多图片：

```text
1张 → 大卡片
2张 → 双列
3–4张 → 网格
5+ → 紧凑网格
```

超过当前可视数量：

```text
+N
```

点击进入Viewer。

---

# 62. Image Viewer

采用真正全屏Viewer。

必须：

```text
左右滑动
当前位置 3 / 20
下载
返回
```

可以设计：

```text
缩略图 → 全屏
```

的连续视觉过渡。

但优先保证稳定、流畅。

---

# 63. 文件卡片

例如：

```text
┌─────────────────────────┐
│  ZIP                    │
│  project-v2.zip         │
│  28.6 MB                │
│                         │
│  23小时后过期      下载  │
└─────────────────────────┘
```

过期：

```text
已过期
```

下载按钮失效。

---

# 64. 输入区

底部Composer：

```text
[＋] [输入内容………………] [发送]
```

点击 `+`：

Bottom Sheet：

```text
照片
文件
```

第一版不要塞：

```text
相机
位置
语音
联系人
表情商店
```

---

# 65. 上传UI

选择文件后：

立即出现本地发送卡片：

```text
project.zip

████████░░
82%

上传中
```

失败：

```text
上传失败
[重试]
```

完成：

```text
可下载
```

发送端不需要等待Windows确认。

---

# 66. Dark Mode

从Design System开始支持：

```text
System
Light
Dark
```

不要等项目做完以后再把：

```text
白 → 黑
```

简单反转。

所有Surface、Text、Divider、Card和状态色都必须来自Theme。

---

# 67. realme GT5 Pro

realme GT5 Pro是：

```text
主要实机验收设备
```

但禁止：

```text
针对该分辨率硬编码布局
```

App仍必须使用：

```text
dp
sp
Window Insets
adaptive constraints
```

保证其他Android手机也能正常运行。

---

# 68. Web视觉

Windows Web与Android：

```text
同一产品
```

但不是：

```text
完全复制手机界面
```

Desktop可以利用宽屏。

建议：

```text
最大内容宽度约800–960px
```

时间线居中。

拖拽文件时：

```text
页面出现清晰Drop Zone
```

---

# 69. Ctrl+V

Windows页面监听：

```text
paste
```

如果Clipboard包含图片：

```text
识别
↓
创建上传Batch
↓
上传
```

如果Clipboard只有文字：

```text
默认粘贴到输入框
```

不能因为实现图片Paste破坏正常文字复制粘贴。

---

# 70. Cleanup

定时清理建议：

```text
每5分钟
```

执行一次。

包括：

```text
过期pairing session
过期upload reservation
失败part文件
24h文件TTL
30天文件消息TTL
```

容量清理除了定时任务外：

**每次创建Upload Batch之前必须同步检查一次。**

否则不能保证1GB上限。

---

# 71. 正在下载保护

开始Download：

```text
active_download_count++
```

请求结束：

```text
active_download_count--
```

容量清理和TTL清理：

如果：

```text
active_download_count > 0
```

则跳过。

下一轮Cleanup再删除。

---

# 72. Server重启

因为：

```text
active_download_count
```

是内存状态：

Server重启：

所有网络下载本身都会断开。

因此无需把active download持久化到SQLite。

---

# 73. 日志

日志可以记录：

```text
request id
API path
status code
duration
file id
message id
device type
```

禁止记录：

```text
Master Token
Browser Token
OWNER_SETUP_TOKEN
QR Secret
完整Authorization Header
Cookie
文件内容
```

---

# 74. 错误代码

不要只返回：

```text
500
```

建议统一业务错误：

```text
FILE_TOO_LARGE
BATCH_TOO_LARGE
TOO_MANY_FILES
INSUFFICIENT_STORAGE
PAIRING_EXPIRED
PAIRING_INVALID
DEVICE_REVOKED
UNAUTHORIZED
FILE_EXPIRED
UPLOAD_INCOMPLETE
```

Android和Web根据：

```text
error.code
```

显示对应UI。

---

# 75. REST Error Envelope

建议统一：

```json
{
  "error": {
    "code": "FILE_TOO_LARGE",
    "message": "File exceeds 300 MB."
  }
}
```

业务逻辑不要依赖英文Message解析。

---

# 76. V1明确禁止新增的基础设施

除非出现实际问题，否则Codex不得自行加入：

```text
Redis
PostgreSQL
Kafka
RabbitMQ
MinIO
S3
Kubernetes
Microservices
GraphQL
gRPC
WebRTC
Firebase
PWA
```

不是因为这些技术不好。

而是：

**本项目不需要。**

---

# 77. 项目目录建议

```text
transfer-assistant/
│
├── server/
│   ├── cmd/
│   ├── internal/
│   │   ├── auth/
│   │   ├── pairing/
│   │   ├── messages/
│   │   ├── files/
│   │   ├── search/
│   │   ├── websocket/
│   │   ├── storage/
│   │   └── cleanup/
│   └── migrations/
│
├── web/
│   ├── src/
│   └── ...
│
├── android/
│   └── ...
│
├── docker/
│
├── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

不要一开始拆多个Git Repository。

---

# 78. Environment配置

建议：

```text
PORT=5757

DATA_DIR=/app/data

OWNER_SETUP_TOKEN=

MAX_FILE_BYTES=314572800
MAX_BATCH_BYTES=524288000
MAX_BATCH_ITEMS=20

FILE_POOL_MAX_BYTES=1073741824

FILE_TTL_HOURS=24
FILE_MESSAGE_TTL_DAYS=30

PAIRING_TTL_SECONDS=120
UPLOAD_SESSION_TTL_MINUTES=30
```

`.env.example`：

只放示例值。

真正：

```text
OWNER_SETUP_TOKEN
```

绝不能提交Git。

---

# 79. 数据备份

由于用户已经明确：

```text
历史文字丢失可以接受
```

V1不开发应用内部备份系统。

但所有持久化内容都集中在：

```text
/app/data
```

因此以后若想备份，只需要备份：

```text
Docker volume / bind mount
```

即可。

---

# 80. 第一阶段验收目标

完成基础技术框架以后，首先必须做到：

```text
Docker启动
↓
5757可访问
↓
1Panel可反代
↓
React页面加载
↓
SQLite初始化
↓
/healthz正常
```

此时：

**暂时不做文件传输。**

---

# 81. 第二阶段验收

完成：

```text
Android初始化
↓
Master建立
↓
Windows出现二维码
↓
Android扫码
↓
Windows配对
↓
旧Windows替换
```

安全模型完全跑通以后：

才开始消息业务。

---

# 82. 第三阶段

文字：

```text
Android → Server → Web
Web → Server → Android
```

完成：

```text
WebSocket
SQLite
分页
删除
FTS5
搜索定位
```

---

# 83. 第四阶段

文件。

---

# 84. 第五阶段

图片与高质量UI。

但是：

Design System必须在Android工程初始化阶段就建立。

不能第五阶段才：

```text
重新换主题
重新改圆角
重新改Typography
```

第五阶段只是对：

```text
图片Viewer
复杂状态
Motion
Polish
```

进行集中完善。

---

# 85. 技术冻结后的核心原则

从现在开始，Codex遇到没有明确说明的需求时：

优先选择：

```text
简单
安全
易维护
单机
单用户
```

而不是：

```text
为了未来扩展预埋复杂架构
```

这个项目的目标依然是：

> 一个每天可以随手使用的私人 Android ↔ Windows 传输助手。

而不是：

> 一个未来准备服务100万用户的即时通信平台。

---

# 86. V1.0.1 已确认交互优化

本节是用户明确确认的 V1.0.1 增量决策。

本节覆盖第69节中 Ctrl+V 仅明确支持图片的范围限制，但不改变其余 V1 产品、技术与安全基线。

## 时间线滚动

Web 与 Android：

```text
初次打开时间线
↓
自动定位到最底部
```

自己发送文本、图片或文件成功后：

```text
始终平滑滚动到最底部
```

收到另一端的新消息时：

```text
当前位于底部
↓
继续自动跟随到底部
```

```text
当前正在阅读历史消息
↓
保留当前阅读位置
+
显示未读新消息提示
```

加载更早消息和搜索定位不得错误触发自动下拉。

## 一键下拉按钮

Web 与 Android 离开时间线底部后，在聊天输入框上方中央显示悬浮下拉按钮。

没有未读新消息时：

```text
显示下拉箭头
```

存在未读新消息时：

```text
显示未读数量
```

点击按钮或用户手动滚动到底部后：

```text
平滑定位到底部
+
清除未读计数
```

## Windows Web 文件粘贴

Windows Web 的 paste 监听扩展为任意剪贴板文件：

```text
Clipboard包含文件
↓
创建上传Batch
↓
沿用现有上传、进度、重试与限制校验
```

如果 Clipboard 只有文字：

```text
保持浏览器原生文字粘贴
```

如果 Clipboard 同时包含文件和文字：

```text
优先处理文件
```

Android 文件剪贴板不属于本次范围，继续使用系统照片选择器和文件选择器。

## 技术边界

本次优化不修改：

```text
后端API
SQLite结构
文件生命周期
持久化目录
Docker与部署架构
```
